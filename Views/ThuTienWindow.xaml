<Window x:Class="QL_CFE_WPF.Views.ThuTienWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:QL_CFE_WPF.Views"
        mc:Ignorable="d"
        Title="Thu Tiền" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Tổng -->
        <TextBlock Text="{Binding TongTienHienTai, StringFormat=Tổng: {0:N0} đ}"
                   FontSize="16" FontWeight="Bold"/>
        <!-- Tiền khách -->
        <TextBox Grid.Row="1"
                 Margin="0,10"
                 Height="30"              
                 materialDesign:HintAssist.Hint="Nhập tiền khách đưa"
                 Text="{Binding TienKhachDua, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}"
                  FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">

            <Button Content="50K" Width="70" Margin="5"
            Command="{Binding ChonTienNhanhCommand}"
            CommandParameter="50000"/>

            <Button Content="100K" Width="70" Margin="5"
            Command="{Binding ChonTienNhanhCommand}"
            CommandParameter="100000"/>

            <Button Content="200K" Width="70" Margin="5"
            Command="{Binding ChonTienNhanhCommand}"
            CommandParameter="200000"/>

            <Button Content="500K" Width="70" Margin="5"
            Command="{Binding ChonTienNhanhCommand}" 
            CommandParameter="500000"/>
            <Button Content="Trả hết" Width="80" Margin="5"
Command="{Binding ChonTienNhanhCommand}" 
CommandParameter="TatCa"/>

        </StackPanel>

        <!-- Tiền thối -->
        <!--<TextBlock Grid.Row="3"
                   Text="{Binding TienThoi, StringFormat=Tiền thừa: {0:N0} đ}"
                   Foreground="Red"
                   FontWeight="Bold"
                   />-->
        <TextBlock Grid.Row="3"  Text="{Binding TienThoi, StringFormat=Tiền thừa: {0:N0} đ}" >
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="Red"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DuTien}" Value="True">
                            <Setter Property="Foreground" Value="Green"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Phương thức -->
        <ComboBox Grid.Row="3"
                  ItemsSource="{Binding PhuongThucList}"
                  SelectedItem="{Binding PhuongThuc}"
                  Margin="0,10"/>
        <!-- Button -->
        <Button Grid.Row="4"
                Content="XÁC NHẬN"
                Height="40"
                Background="Orange"
                Command="{Binding XacNhanThanhToanCommand}"/>



    </Grid>
</Window>
