<Window x:Class="QL_CFE_WPF.Views.ChonBanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QL_CFE_WPF.ViewModels"
        mc:Ignorable="d"
        Title="Chọn Bàn" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <ItemsControl ItemsSource="{Binding DanhSachBan}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Content="{Binding TenBan}"
                        Width="80"
                        Height="80"
                        Margin="5"
                        Command="{Binding DataContext.ChonBanCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</Window>
