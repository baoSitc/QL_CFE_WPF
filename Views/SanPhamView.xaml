<UserControl x:Class="QL_CFE_WPF.SanPhamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QL_CFE_WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:MainViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FORM -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBox Width="150"
                 Margin="5"
                 Text="{Binding TenSP, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Width="100"
                 Margin="5"
                 Text="{Binding Gia, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Thêm" Command="{Binding AddSanPhamCommand}" Margin="5"/>
            <Button Content="Sửa" Command="{Binding UpdateSanPhamCommand}" Margin="5"/>
            <Button Content="Xóa"
                Command="{Binding RemoveSanPhamCommand}"
                CommandParameter="{Binding SelectedSanPham}"
                Margin="5"/>
        </StackPanel>

        <!-- GRID -->
        <DataGrid Grid.Row="1"
              ItemsSource="{Binding SanPhams}"
              SelectedItem="{Binding SelectedSanPham}"
              AutoGenerateColumns="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã" Binding="{Binding MaSP}"/>
                <DataGridTextColumn Header="Tên SP" Binding="{Binding TenSP}"/>
                <DataGridTextColumn Header="Giá" Binding="{Binding Gia}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
